{
  "name": "deazl-v2",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/web build",
    "mobile": "bun run --cwd apps/mobile start",
    "typecheck": "bun run --cwd apps/web typecheck && bun run --cwd packages/domain typecheck && bun run --cwd packages/supabase typecheck",
    "supabase:start": "bunx supabase start",
    "supabase:stop": "bunx supabase stop",
    "supabase:status": "bunx supabase status",
    "supabase:types": "bunx supabase gen types typescript --local > packages/supabase/src/database.types.ts"
  }
}
